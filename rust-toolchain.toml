# The §25.8 recorded toolchain for the native/ filesystem boundary. Pinned
# rather than floating: the artifact ships committed per target, so the
# compiler that produced it is part of what "rebuilds and verifies it" means.
[toolchain]
channel = "1.96.0"
targets = ["x86_64-unknown-linux-gnu"]
profile = "minimal"
