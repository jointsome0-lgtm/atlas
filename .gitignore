# Private data kinds
*.sqlite*
*.db*
*.jsonl
.env
.env.*
engine.pin
copies-manifest
delivery-registry

# Instance-shaped directories
atlas/
data/
state/
intake/
!fixtures/intake/
!fixtures/intake/vera-example-batch.json
graph/
plans/
runs/
secrets/

# Local agent and tool state
.claude/
.codex/
.agents/
.atlas-lock
__pycache__/
node_modules/

# Cargo's scratch for the POSIX boundary. The shipped artifact is NOT here —
# §25.8 commits it per target under native/*/lib/<triple>/, so a plain checkout
# runs everything without a Rust toolchain, and CI rebuilds and compares it.
native/*/target/
