# Private data kinds
*.sqlite*
*.db*
*.jsonl
.env
.env.*
engine.pin
copies-manifest
delivery-registry

# Instance-shaped directories
atlas/
data/
state/
intake/
!fixtures/intake/
!fixtures/intake/vera-example-batch.json
graph/
plans/
runs/
secrets/

# Local agent and tool state
.claude/
.codex/
.agents/
.atlas-lock
__pycache__/
node_modules/

# Rust build products for the POSIX boundary (native/). The library is built
# from source by 'bun run build:native'; nothing compiled is tracked.
native/*/target/
