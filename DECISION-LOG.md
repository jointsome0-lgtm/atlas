## Decision Log

Format: `YYYY-MM-DD — decision in one phrase; rejected alternative and why. (#issue, sha)` — full argumentation lives in the commit body and issue; do not restate it here.

- 2026-07-03 — Keep the SDD as a single file navigated via the § Index (map-as-interface); a physical split into a map file plus per-section files is deferred until after the structural dedup pass, and if done, must be a purely mechanical commit (concatenated section files must reproduce the original). Revisit triggers: code lands and sections graduate into living docs/tests; full-pass reviews start hitting context limits; parallel per-section editing becomes the norm; the file exceeds ~30–40K tokens despite dedup. Rejected alternative: splitting now — point reads already load only the needed section (§ Index + grep), a split does not fix the actual pain (cross-section duplication and drift), and splitting before dedup would migrate content about to be merged or deleted.
- 2026-07-04 — Added §31 Key Invariants (candidates): six hard rules distilled from §1/§4/§5/§9.9/§11/§12.2/§14/§15/§17.2/§19/§25.3 as a safety net for dedup passes, explicitly marked pending grill/approval. Rejected alternative: approving invariants in the same pass — they deserve a dedicated grill session; unapproved candidates are enough to guard dedup against silent loss.
- 2026-07-04 — Structural dedup, no decision changes (version stays 0.1): §22 merged into §12.3 (plan node, route/concept/probe ids; number §22 retired, never reused), §2 layer-definition block and uploaded-plan description replaced with refs to §5/§6/§12.3 (repo name moved to §12.3), §23.2 closing mantra compressed to a one-liner referencing §15, §29 compressed to a phase→content table with scope left to §26. Rejected alternative: leaving duplicates as is — copies drift further apart and cost more with every polishing session.

- 2026-07-04 — Executed the deferred physical split (see 2026-07-03): `SDD.md` is now the map (§ Index + numbering rules), each top-level § lives in `spec/NN-slug.md`, decisions in `DECISION-LOG.md`; § numbering and all content unchanged — head + `spec/` files + log concatenate back to the pre-split file byte-for-byte (verified). Trigger: context root — file boundaries enforce the section-scoped reading the convention only requested (the exp2res monolith already exceeded the default 2000-line read window, silently truncating whole-file reads; split kept symmetric across SDD-stage repos). Rejected alternative: single file + convention — conventions don't bind agents, structure does.
- 2026-07-04 — Renamed trajectory→trail in §8/§10/§20/§26: the sacred layer gets exactly one name — the ontology's (§6). Rejected: renaming the ontology to trajectory — the design language (§1, §5.2, §30) is built on "trail". (#6, a8b48e5)
- 2026-07-04 — §21.2 title aligned with the §12.3 plan node; rule: `id` is a stable slug, `title` comes from the plan's own heading. Rejected: keeping "Learn Basics SWE" — a filename-derived placeholder. (#3, a433295)
- 2026-07-04 — §19 now references §4's ban list as canonical, keeping only checker-specific additions. Rejected: a hand-synced copy — forked ban lists diverge silently. (Original drift claim was a review misread — pure dedup; #8, 4974f95)
- 2026-07-04 — §10.1: added `plan` (§12.3/§27 require the node), removed `snapshot` (defined nowhere), stated the id convention. Rejected: defining snapshot — nothing consumes it, speculative types are §28.3's risk. (#7, 877d786)
- 2026-07-04 — Evidence pipeline closed: Artifact/Encounter storage in `state/`, creation steps in §13.2, builder reads in §20. Rejected: per-record files under `atlas/` — event logs, not curated notes; encounters folded into artifacts — §9.10/§25.3 need them distinct. (#5, 72ca599)
- 2026-07-04 — Current position is derived from recent trail heads; `current-position.yaml` dropped. Rejected: keeping the stored file — a second source of truth beside the sacred trail (§5.2), and singular against multi-focus movement. (#9, b892ea3)
- 2026-07-04 — §9.1 Concept frontmatter stripped of `status`/`stability`/`material_roles`; concept files carry identity/links/content only. Rejected: defining the fields — they duplicate the state model or reintroduce global roles (§5.5/§11/§28.5). (#10, ff3fc0f)
- 2026-07-04 — §14 got real rules: evidence→exposure mapping (monotone max, `supports_state_updates` only, encounters cap at `read`), review-gated confidence/clarity/coverage, derived freshness (30/90d defaults), material-state schema; SDD 0.1→0.2. Rejected: unified evidence/exposure scales — an artifact's reviewed-ness ≠ a concept's taught-ness; auto-confidence from probes — §26.2/§17.2 forbid it. (#4, 5eec2cc)
- 2026-07-04 — Log format enforced: decision + rejected alternative (+ why) in ~40 words with `(#issue, sha)` refs — argumentation already lives in commit bodies and issues; today's seven entries compressed as the example. Product local-first (§24/§25.1) binds Atlas data, not the dev process, which is GitHub-canonical anyway (AGENTS.md: findings → issues). Rejected: paragraph entries — agents clone the visible style, growth compounds; retro-editing older sessions' entries — history-rewriting outweighs savings there.
- 2026-07-04 — §8 rewritten as placement principles + normative skeleton: kept every path other §§ rely on, cut content predictions (pre-named concepts/probes/routes, ten `docs/*_MODEL.md`, four pre-named ADRs, stray `graph/build_graph.py` duplicating §20's builder). Rejected: principles-only — the path map proved load-bearing this session (#5/#9 landed in it); full tree — content fiction diverges the moment code lands. (#1)
- 2026-07-04 — §17 cut from ten agent roles to four (plan-importer, artifact-observer, field-cartographer, state-auditor): cartographer absorbs atlas-architect + material-analyst, auditor absorbs red-team-reviewer; builder/viewer are code, not roles; codex-coordinator restated §18. Re-splitting requires a demonstrated overload. Rejected: keeping ten — an org chart specified before any role has run once is §28.3's complexity risk in agent form. (#2)
- 2026-07-04 — §4: understanding is characterized, never measured or ranked — no aggregate skill level or score per concept cluster, direction, or field; revisit if assessment tech matures. Rejected: game-style skill levels (the pre-SDD original desire) — unmeasurable today, and a single number is a pressure re-entry point (§28.1).
- 2026-07-04 — §19/§31.1 (grill §31): forbidden-term scan binds Atlas's voice only — full text where Atlas speaks (README/CLAUDE/AGENTS, docs/, viewer/), keys + closed-enum values in atlas/ and state/, user free text and plans/imported/ exempt. Rejected: dropping the checker for schema-validation-only — §28.1's top risk loses its one deterministic guard; scanning user text — the checker would bite the sacred trail (§5.2).
- 2026-07-04 — §5.2/§9.9/§20/§31.2 (grill §31): trail protection is actor-scoped — automation appends and proposes only (user confirms), the user edits/deletes freely, builder warns-and-skips sources deleted by the user. Rejected: absolute immutability with amendment segments — privacy deletion impossible, enshrined mistakes are a guilt mechanism (§3); actor left unnamed — the first importer author would decide implicitly.
- 2026-07-04 — §5.3/§12.2/§31.3 (grill §31): plan import is state-neutral both ways — new concepts start unseen/unknown/vague, existing state untouched, plan self-claims ("already know X") surface only as import-report proposals confirmed by the user (§14.6). Rejected: auto-applying self-claims — stale self-deception, §28.2 overclaim path; keeping the "unless prior artifacts exist" parenthetical — a no-downgrade rule masquerading as upgrade permission.
- 2026-07-04 — §11/§31.4 (grill §31): the global-role ban covers derived outputs too — no materialized primary/supporting aggregates in graph/, viewers compute context-labeled aggregates at render time. Rejected: derived-ok aggregate fields on the Material node — §28.5's validator would need an allowlist, the exact re-entry path for global roles.
- 2026-07-04 — §31.5 (grill §31): candidate stops forking the evidence list — states the split (exposure via §14.5 monotone mapping; confidence/clarity/coverage via §14.6 propose→confirm) and defers evidence kinds to §14.6/§25.3, whose reconciliation is #15. Rejected: enumerating kinds in §31 — a third hand-synced copy of a list already disagreeing across two sections.
- 2026-07-04 — §24/§31.7–8 (grill §31): §31 grew invariants 7 (local-first, refined: machine is the only canonical store, nothing sent on Atlas's initiative; user-started agent sessions are the one legal transit, secrets never enter them) and 8 (derived is never stored — backs #13); §24 hardened from "MVP should not" to standing rules relaxed only via Decision Log. Rejected: 7 as "data never leaves the machine" — agents transit data to model APIs every session, the absolute was false on day one (user's catch).
- 2026-07-04 — §31 approved (grill verdict APPROVED_WITH_NOTES): eight invariants, "candidates" dropped from title/file, sync rule added — a cited § changed by explicit decision updates its mirroring invariant in the same commit; notes: #11 and #15 flow into §31.6/§31.5 through that rule. Rejected: pending until #11/#15 close — invariants defer to canon so approval freezes nothing, while a soft keel under-protects against the dedup passes §31 exists to guard.
- 2026-07-04 — §8/§9.10/§13.2/§17.1/§20/§26.1 (#13, 6154a9e): influence is derived only — the builder computes it at build time into `graph/atlas-graph.json`; `state/influence-field.yaml` dropped, the observer no longer proposes influence updates. Rejected: materialized cache with an explicit write rule — still a second source of truth (§31.8), the reasoning that killed `current-position.yaml` (#9).
- 2026-07-04 — §9.4/§15/§19/§31.6 (#11): `pressure` field dropped from all schemas; the key itself is checker-banned as data (§19), pressure-freedom stated as law in §15 prose; §31.6 synced per the approval-note rule. Rejected: keeping `pressure: none` as a machine-checkable tripwire — a field waiting for its second value; banning the key is the simpler predicate and blocks silent re-entry of the pressure dimension.
- 2026-07-04 — §9.6/§9.11 (#15): a probe response is an artifact with an optional `probe:` link; Probe got a schema (concepts, source_plan?, status; body = the check) with deliberately no `expected:` field. Rejected: a ProbeRun record — double-writes one event, and its outcome field is a gradebook re-entry point.
- 2026-07-04 — §9.12/§6/§14.6/§25.3/§31.5 (#15): canonical evidence kinds live in §9.12 — exactly {artifact, encounter, question}; manual note = note artifact, agent review = propose→confirm mechanism, manual state edit = self-proposal citing a note; all other mentions cite §9.12 without re-enumerating. Rejected: canon in §14.6 or §25.3 — a schema list kept outside §9 is where the fork grew.
- 2026-07-04 — §8/§9.13/§13.2/§14/§20/§25/§31.8 (#15): state/ = append-only JSONL journals only; resolved proposals land in `state/decisions.jsonl`, pending ones are derivable and never stored; concept-state/material-state files dropped — the §20 fold derives understanding and material state. Rejected: basis fields on stored state — a second source of truth plus a reconciler, and rejections stay homeless; SQLite as canon — binary breaks git-diff audit (§25.2), legal only as a derived index under graph/.
- 2026-07-04 — §13.2 (#15): "ask only when ambiguous" scoped to observation records; review-gated proposals always wait for a recorded decision (§9.13), rejections are not re-asked without new evidence; trail segments are appended, not proposed (§31.2). Rejected: unscoped step 10 — it read as permission to apply confidence changes silently, against §14.6.
- 2026-07-04 — §25.5 (#17): domain generality encoded as a design test — the core (journals, evidence/decisions, fold, influence, frontier, §31 invariants) is domain-parameterized; per-domain surface = field semantics, state scales, observer interpretation, probe/material meaning; a second domain (candidate: body atlas) arrives as its own field + scales, never a core fork; no abstraction machinery until it is real (§28.3). Rejected: redefining §6 Field or adding a §5 principle now — canonizing generality before a second domain runs is designing on fiction; grill that in the body-atlas session.
- 2026-07-04 — §9.9/§13.2/§15.1 (#14, lens #17): trail topology is emergent — `from` = evidenced causal origin(s), 0..n (absent = landing, several = synthesis step); one artifact may yield several segments (star day = N from-less landings sharing `via`); position = simultaneous heads per direction. Rejected: `from` = latest trail head — fabricates continuity in the sacred layer (§5.2); mandatory `from` — first movement unrecordable; `to`-lists — breaks one-step-one-destination (§6); a separate Visit type — machinery without need (§28.3).
- 2026-07-04 — §9.14/§10.2/§11.4/§31.4 (#12): «B helps understand X» = standing directed `supports` link, authored on the receiving side (`supported_by` on the material or its parts[] entry), endpoints material|part; directions independent (help A→B ≠ help B→A), cycles normal, symmetric relation forbidden. Rejected: a fourth §11 context — degenerate primary slot, an edge in context clothes; helper-side `helps:` / separate relations file — authoring locus ≠ need locus, a relation is not a §6 entity.
- 2026-07-04 — §14.9/§9.13/§10.3/§12.2/§13.2/§17.1 (#12): edge weight is learned, never authored — supports links start unassessed and move only through the §14.6 gate as StateDecisions with edge targets (`supports:<src>-><dst>`); concept_edges authored weight = import-time hypothesis, decisions override; observer proposes weight, cartographer owns existence/endpoints. Rejected: authored weight on supports — «learned, not imported» violated; mechanically derived weight — counts episodes instead of judging help; `weight: none` as removal — existence is a curated file edit.
- 2026-07-04 — §9.2/§14.8 (#12 grill): material understanding is never stored — material state = contact (material/part keys independent, no part→material aggregation); `overall_concepts` = the material's through-line (motif), moved only by cross-part synthesis artifacts. Rejected: a material-understanding scale / auto-aggregation «all parts read ⇒ material understood» — imported understanding (§5.3); a part-order arc entity — a route inside the material.
- 2026-07-04 — §25.6 (#12 grill): Durable NFR — the repo is a git repository, version history is the recovery mechanism, durability beyond the machine = user-initiated copy of the whole repo (Atlas never syncs on its own, §24). Rejected: denormalizing journal values into files as «backup» — a second source of truth (§31.8); durability is a storage concern, not a model concern.
- 2026-07-04 — §9.3/§10.1 (#12, lens #17): a part is a named sub-unit of any shape (section, chapter, timestamp range, module, block); `section-slug` → `part-slug` in the id note; supports machinery is shape-neutral (drill video ──supports──▶ squat block works). Rejected: a `locator` field — YAGNI, the part title carries «12:30–18:00» / «Week 3».
- 2026-07-06 — §32 (#17): body atlas instantiated as the second domain — two-layer field: zones (anatomical regions carrying adaptation/condition, silhouette geometry) + patterns (movement skills as concept-kind nodes); pattern→zone = `concept_edges` species with `role: loads`; materials stay materials (programs, videos). Rejected: patterns as materials — technique is understanding, material state is contact-only (§14.8); a body data entity — viewer projection over the one graph (§31.8).
- 2026-07-06 — §32.2/§31.5 (#17): zone state = monotone contact ladder + gated capacities (strength/endurance/mobility) + condition + derived freshness; down through a probe, never through a curve — declines need measurement evidence (probe, diary line, medical record) through the §14.6 gate; §31.5 now says changes require evidence in both directions. Rejected: fitness-tracker auto-decay — a population curve asserted as personal measurement, with nothing citable under §9.12.
- 2026-07-06 — §32.2 (#17): `condition` gets a fifth value `chronic` — rendered in shadow (subtle shading, no alarm badges, §25.4), always read at suggestion time; care notes = curated markdown in the zone file. Rejected: permanent `restricted` — loses «hurts now» vs «careful always»; a structured care-rule schema — machinery in advance (§28.3).
- 2026-07-06 — §32.3 (#17): patterns take knowledge scales verbatim with a motor exposure ladder (unseen→touched→studied→tried→drilled→reviewed; `tried` > `studied` — doing is deeper contact than reading). Rejected: separate technique scales — technique is understanding of a movement, §14.2–§14.4 apply as-is.
- 2026-07-06 — §32.4 (#17): capture = three-tier spectrum (sacred free-line floor / semi-structured log / device export via the #19 generic intake) through the §13 observer; diary numbers are measurements, sensations are evidence, questions extracted; device telemetry evidence-only and user-initiated (§31.7); `streak` checker-banned (§19). Rejected: mandatory structured fields — each required field doubles capture cost and kills the habit; ambient device sync — Atlas initiates nothing.
- 2026-07-06 — §15.3/§31.6 (#17, user's finding): guilt redefined — exploitation of an unfulfilled plan for retention, not negative information; honest-lever rule: a true dependency may be stated plainly given a real edge + the user's own anchor (direction/question) + cited evidence; retention mechanics banned as a mechanism. Rejected: adjacency-euphemism-only wording — flattering manipulation («тешим своё мнение»); deficit language without the user's anchor — no truth value («lagging» — behind what?).
- 2026-07-06 — §32.6/§25.5 (#17): the medical layer decomposes onto the existing ontology (reference = curated content; measurements/diagnoses = probe-linked artifacts; current condition = derived; individual map = routes respecting condition) plus a data sensitivity class — medical files excluded from agent context by default (§31.7); §25.5 per-domain surface grew field geometry, suggestion-time constraints, sensitivity classes; SDD 0.3→0.4. Rejected: a medical subsystem/second graph — a second source of truth (§31.8); deferring medicine entirely — the sensitivity class must exist before the first diagnosis lands in a diary.
- 2026-07-06 — §33 (#19): external exchange boundary — blind peers, adapters outside; intake = §9.12 evidence kinds + plans in atlas vocabulary (adapters translate; verbal lane for the rest), evidence-only into the existing §12/§13 flows, batch provenance `intake:<source>/<batch>`, `intake/` checker-exempt (§19); SDD 0.4→0.5. Rejected: per-peer formats in atlas — blindness lost; adapter writes into `state/` — bypasses observer resolution, provenance, and the report.
- 2026-07-06 — §33.4 (#19): state snapshot = emitted view atlas never reads back (§31.8 intact), self-describing scales, evidence refs + confirmed decisions, closed included-set; frontier/routes never export, §32.6 class off by default. Rejected: exporting frontier — re-presented suggestions are a todo list (§31.6); numeric ladders — a mastery score in transit (§4).
- 2026-07-06 — §16.4 (#19): viewer embedding = stable hash URLs (mode + focus), the URL is the whole input, same render top-level or iframed; params only added, renames need a Decision Log entry. Rejected: a postMessage embed API — shell-specific code in atlas, coupling the viewer to a host it must not know.
- 2026-07-06 — §33.2 (#20 Codex): intake idempotency is per record — provenance `intake:<source>/<batch>#<n>` (n = position in records), a batch id names one immutable delivery (changed content ⇒ refused, corrected batch ⇒ new id), partial runs resume at the first unrecorded record. Rejected: batch-level key — a retry after a partial run either skips unprocessed records or duplicates processed ones; a processed-batch marker — batch-granular only, bookkeeping outside the journals (§31.8).
- 2026-07-06 — §33.4 (#20 Codex): the §32.6 default exclusion is provenance-based — the sensitivity class travels from an evidence record to everything derived from it (state/decisions, evidence refs, trail via, extracted questions, material contact), so no snapshot section leaks it. Rejected: excluding only evidence refs + zone condition — sensitive text exits through questions and trail; excluding whole sections — drops non-sensitive content with it.
- 2026-07-06 — §19 (#20 Codex): §4's measurement ban enters the structure scan as key/enum stems (score, rating, percent, rank, level, progress, completion, mastery) — the pressure mechanism (#11); makes §33.4's "checker bans the key stems" claim true. Rejected: weakening the §33.4 claim instead — leaves the snapshot's sharpest exclusion guarded by prose alone.
- 2026-07-06 — §33/§33.2 (#20 Codex r2): `source` is an opaque namespace — atlas attaches no semantics, never branches on it (scopes intake/, provenance, idempotency only); the blindness claim now states what is guaranteed: no peer schema/code, no interpretation of peer identity — the label itself is user's voice. Rejected: banning peer-named labels — the checker would police user memory (§5.2), and blindness-by-vocabulary is unenforceable anyway.
- 2026-07-06 — §33.2/§8 (#20 Codex r2): intake receipts — `state/intake.jsonl`, one row per processed record (outputs first, receipt last); the receipt is the one processed-marker (covers plan records, which append no journal rows) and is provenance, not evidence (§9.12 untouched, §20 fold ignores it); an interrupted partial (rows, no receipt) goes to the batch report, never silently reprocessed — the observer interprets (§21), a second pass may differ. Rejected: any-output-row-present as the marker (r1) — plans re-import forever, multi-output records resume-skip their tail.
- 2026-07-06 — §16.4 (#20 Codex r2): embed mode slugs are canonical and closed — {field, material, route, trail, influence, state, frontier, question}, defined in §16.4 only; a new §16.1 view adds its slug. Rejected: kebab-casing §16.1 names — "Personal Trail View" → personal-trail-view against the examples' trail; a derivation rule over prose titles is not a URL contract.
- 2026-07-06 — §33.4 (#20 Codex r3): material contact state exports with its evidence refs (encounters) like any state entry — §33.1's "state as evidence, with provenance" admits no exceptions. Rejected: dropping material contact from the export — the honest fix is provenance, not amputation; a consumer tracing summarized back to its encounter is §25.3 crossing the boundary.
- 2026-07-06 — §33.2 (#20 Codex r3): receipts become an opened/done pair (opened before any output, done after the last) — partial detection = opened-without-done, covering non-journal outputs (plan originals, candidate files) with no output scanning; interrupted records are never silently reprocessed or overwritten. Rejected: detection by output presence (r2) — a plan partial leaves no journal row to find; deterministic imported-plan paths as the detector — a second mechanism for the same fact (§28.3). (Marker name superseded by r5: `processed`, not `done`.)
- 2026-07-06 — §19 (#20 Codex r4): identifiers are exempt from every structure-scan term rule — a node id (§10.1) is user vocabulary (§5.2) wherever it appears, map keys included: concept:isolation-level must not trip level, nor zone:blood-pressure the pressure ban (a pre-existing hole the stem review exposed). Rejected: reshaping snapshot state into id-field lists — bending a format to dodge a checker bug.
- 2026-07-06 — §33.2 (#20 Codex r4): source and batch are slugs (lowercase letters, digits, hyphens) — intake/<source>/ paths and <source>/<batch>#<n> receipt keys parse unambiguously by construction; violating deliveries refused in the batch report. Rejected: an escaping scheme — two encodings of one identity invite the collisions they exist to solve.
- 2026-07-06 — §33.4 (#20 Codex r4): scales are complete per node kind — every dimension an exported entry carries has its ladder in the snapshot (motor exposure beside knowledge exposure, full §32.2 zone set); a value whose ladder is absent makes the snapshot malformed. Rejected: scoping body nodes out of the export — the second domain is not a second-class citizen (§25.5).
- 2026-07-06 — §33.2 (#20 Codex r5): the receipt completion-marker is `processed`, not `done` — state/intake.jsonl is atlas's own voice, structure-scanned by §19, where §4 bans done as data; the boundary's bookkeeping obeys the law it enforces. Rejected: a checker exception for receipts — the first legal hole in the no-task-state invariant (§31.1).
- 2026-07-06 — §33.4 (#20 Codex r5): scales gain material {depth_reached: the §9.7 encounter-depth ladder} — the r4 per-kind completeness contract applied to material contact; the example had violated its own rule. Rejected: omitting material state from the export — r3 already settled provenance-not-amputation.
- 2026-07-06 — §33.2/§32.4 (#20 Codex r6): sensitivity is declared, never inferred — optional `sensitivity: "medical"` per record or envelope default (record wins), values = the §32.6 classes (closed, new class = Decision Log); classification is the adapter's translation burden (§33.1 — source stays opaque), the verbal lane is classed by the observer; the class persists onto every derived journal row — the bit §33.4's exclusion and §32.6's agent-context rule follow. Rejected: inferring the class from source — breaks source opacity; classing at export by content inspection — a guess exactly where a guarantee is promised.
- 2026-07-06 — §24/§33.2 (#20 Codex r7): intake/ never enters default agent context — a delivered original may carry §32.6-class text whether or not its records were marked, so the raw file is covered by construction; the one legitimate reader is the user-initiated flow processing a batch (§31.7); a default, not an absolute (unlike the secrets ignore paths). Rejected: per-file quarantine/marking of sensitive originals — splitting a mixed batch breaks delivery immutability (r4) and trusts the marker exactly where it may be missing.
- 2026-07-06 — §33.1 (#20 Codex r8): the inward invariant is "evidence and plans, never state" — a plan record is the one non-evidence kind (§9.12), creating candidate structure through §12 (§31.3); "evidence only" read literally told an implementer to reject the very records §33.3 imports. Rejected: keeping the slogan with a footnote — an invariant that needs an asterisk is not one.
- 2026-07-06 — §33.3/§8 (#20 Codex r8): a sensitive plan keeps its class across the copy — original to plans/imported/<class>/ (placement, never an edit of an as-delivered document), the derived SuggestedRoute carries sensitivity in frontmatter; the §32.6 default exclusion follows both; candidate stubs stay plain (ids and links are structure) until the user curates otherwise (§5.2). Rejected: quarantining sensitive originals inside intake/ — two homes for imported plans; frontmatter on the original — mutates the audit copy.
- 2026-07-06 — §9.8/§9.13/§13.2/§20/§8/§14 (#16): question status is review-gated event data — the creation record (state/questions.jsonl, renamed from open-questions.jsonl) never mutates; transitions are StateDecisions (dimension: status) citing evidence (stale cites the user's own note); the §20 fold derives current status. Rejected: status-transition records in the questions journal — a second decisions journal with its own fold; declaring questions mutable as a §8 exception — loses the trace.
- 2026-07-06 — §6/§10.1/§10.2/§8/§32.1/§20 (#21): the core registries learn the second domain — §6 de-dialected (Field = a domain's region space, State = current state on the field's scales, Probe reveals state: understanding or capacity) and gains Zone/Pattern; §10.1 gains zone/pattern, §10.2 gains loads and demonstrates (a knowledge-domain hole §9.3 had already exposed); §8 skeleton gains atlas/zones/ + atlas/patterns/, §20 step 1 reads them; loads edges are authored in pattern frontmatter like a part's concept_edges; rule stated in §6: a domain design pass extends the canonical kind/edge lists in the same commit. Rejected: per-domain registries §10 defers to — scattered canon is where forks grow (#8, #15).
- 2026-07-06 — §9.6/§14.5/§32.2/§32.3 (#21): evidence→ladder mappings for the body scales — one §9.6 strength list (gains performed, drilled) mapped per ladder, each mapping beside its ladder: §14.5 knowledge (performed/drilled → applied), §32.3 motor (paper work is study — applied/explained → studied, only performed/drilled mean doing; reviewed = the performance survived review; encounters cap at studied), §32.2 zone contact structural (targeted work or loads: high from a performed pattern → loaded, incidental/weaker → touched, probe answer → probed; study and encounters never move contact). Rejected: a separate body strength enum — a fork of the one evidence vocabulary; mapping applied → tried — a program note would fabricate physical contact.
- 2026-07-06 — §16/§20/§10/§32 (#22): the viewer owns the second geometry — one view set for every field (§16.1); §16.2 splits semantics per geometry (node-link: knowledge + body patterns; silhouette: zones — indicators onto curated figure regions at view time, chronic in shadow §32.2); §16.4 keeps the closed mode set: geometry is a property of the field, the focused node selects the field, never a new slug; the zone → figure region mapping is authored as figure_region in zone frontmatter, §20 step 12 embeds it under projections (§10), so "reads atlas-graph.json and nothing else" stays true; the figure artwork is a viewer asset like its stylesheet. Rejected: new body mode slugs + a second curated viewer input — amends the published URL contract and forks the single-input rule; a standalone projection file — a second registry of which zones exist.
- 2026-07-11 — §29/§20/§8/§10 (spike): Phase 1 implementation starts — scripts/build_atlas_graph.py (stdlib-only §20 subset: curated parsing steps 1-5, reference validation step 11, deterministic §10 JSON with Phase 3-4 keys emitted empty), §8 skeleton directories, invented demo data in the SDD's own example domain; AGENTS.md design-stage line replaced with the implementation rule (code cites its §). Rejected: delegating the spike to Codex — first executable cut of a 33-section SDD is taste-heavy (which §20 steps are Phase 1, how strict the id/status validation) and sets patterns exp2res will copy; full §20 pipeline in one pass — folding/influence belong to Phases 3-4 and would force journal semantics before §13 is exercised.
- 2026-07-14 — Standing carve-out (grill #35): purge (history rewrite) is a named operation breaching §8/§25.6 no-truncating-compaction; triggers = §32.6 class-default + owner-declared (§34.1); per-event notes stay instance-side (state/purges.jsonl) — this public log never accumulates purge metadata. Rejected: per-event entries here — purge history must not leak into a public repo. (#35)
- 2026-07-14 — §34 added, SDD 0.5→0.6 (grill #35): purge closure is provenance-computed with the sole-basis rule — rows removed whole or kept whole (dangling refs), content never edited; no tombstone kind or dangle registry (revisit: live-instance noise). Rejected: editing via/evidence lists — automation-falsified memory (§31.2/§25.3); deleting every touching row — #35's own silent thinning. (#35)
- 2026-07-14 — §34.4–§34.5/§9.1/§10.1/§20 (grill #35): id retirement via formerly: on the survivor (rename=merge, n→1, union on absorption; retired ids never reused; builder resolves at fold, journals untouched); split is curation — a 1→n redirect fabricates state (§5.3). Rejected: redirect stub files — editable clutter; a renames journal — curation in state/; redirect-to-primary/all — imported understanding. (#35)
- 2026-07-14 — §34.6/§32.6/§33.2 (grill #35): classed records and files take date-serial ids at creation (the trail-segment pattern); sensitive adapters take neutral source slugs; unclassed ids stay descriptive — a telling id expands an owner-declared rewrite set to its refs. Rejected: random suffixes — unreadable, the date sits in row fields anyway; neutral euphemisms — drift back to telling. (#35)
- 2026-07-14 — §34.3/§8 (grill #35): per-event purge note = state/purges.jsonl row {date, classes?} — append-only, content-free, provenance-not-evidence (receipts precedent); anchors date-based export invalidation. Rejected: a decisions.jsonl row — breaks the StateDecision schema (mandatory §9.12 evidence); a curated markdown note — prose attracts content. (#35)
- 2026-07-14 — §8/§25.1/§25.6/§31.8 (grill #35, sync rule): "the repo" = the private instance repository (engine/instance split; the layout table is the composing shell's); emitted graph and snapshots untracked in the instance — recovery of a derivable file is a rebuild, not a checkout. Rejected: stating it only in §34 — §25.6 keeps overclaiming history-as-recovery and §31.8 breaks its own sync rule. (#35)
- 2026-07-14 — §34.2 tooling (grill #35): closure is defined mechanically but driven manually-first — the composing shell's runbook runs filter-repo by hand; the engine may later ship a read-only closure lister (trigger: first real purge or §32.6-data admission); the rewrite itself is not scripted at this stage. Rejected: a full purge script now — an untested history-rewriter is a false safety guarantee (§28.3). (#35)
- 2026-07-14 — §9.1 keeps dimension names + `last_seen` (data, not scale) and cites §14.1–§14.4/§14.7 for values — the §19→§4 pattern. Rejected: keeping the copy — forked enums diverge silently (#8/#15 precedent). (#24)
- 2026-07-14 — Drift batch executed: §7 redrawn around journals + the §20 fold (no State Updater; one builder output); §14.5 gains `reviewed → applied` (reviewing applies judgment to the work); §17.1 observer aligned with §13.2/§31.2; §5.3 example reworded as derived default; provenance spelling unified on §17's hyphenated role names. Rejected: leaving prose written before later decisions — each copy is an implementer trap. (#25)
- 2026-07-15 — §33.3/§34.2/§34.6 (#50 Codex F4): candidate stubs from a classed input carry the class like the route — §34.6 date-serial ids and the §33.4 exclusion follow with no new mechanism, and the stub sits in the input's purge closure while the input is its only provenance; adoption = the user re-authoring the stub (§5.2), which removes the class. Scopes 2026-07-06 "#20 r8": plain stubs hold for unclassed inputs — when the source is classed, a slug from its text is content, not structure. Rejected: date-serial ids alone — the title in the body survives the rewrite outside the closure; forbidding candidate structure from classed inputs — a route with nothing to reference makes classed import useless. (#50)
- 2026-07-15 — §34.2 (#50 Codex F8): a multi-basis survivor keeps its persisted §32.6 class — a named residual ("classed provenance existed at this row"), instance-side only since the class itself holds the row inside the §33.4 default exclusion; the closure review may pull a survivor whose retained association is telling into the set by declaration (whole, never edited). Rejected: deleting class-tainted survivors whole — kills rows the user's live ordinary memory rests on; stripping the class — automation-edited row content (§31.2), and the declassed row starts exporting. (#50)
- 2026-07-15 — §34.4/§34.6 (#51 Codex F2): purge overrides retirement — a purged retired id leaves the survivor's formerly list as part of the rewrite set (its journal refs are already in the extended set); reuse protection falls back to the §34.6 discipline clause — no purged-id list exists anywhere, by construction. Rejected: keeping the entry — a purge that leaves the id in current state is self-defeating; a digest registry — a telling-slug dictionary brute-forces it, digest ≈ id (§28.3). (#51)
- 2026-07-15 — §33.2 (#51 Codex F7): receipts are a closed schema — opened and processed both {intake, date}: receipts survive every rewrite, so the row is content-free by construction, never by care; disposition detail moves to the purgeable batch report, outputs stay recoverable via journal rows' intake: keys; sensitive sources take date-serial batch ids, mirroring §34.6 (source-only neutrality left feed-1/positive-biopsy-july#7 conformant). Rejected: a prose "no content in disposition" rule — the longest-lived row guarded by one sentence (the §19-stems precedent); purgeable receipts — a stale redelivery reimports what was purged. (#51)
- 2026-07-15 — §20 (#51 Codex F9): dangle tolerance classifies by the ref's origin, never the target's kind — refs in retained journal rows warn-and-skip whatever they target (questions and curated nodes included: §34.2 promises exactly such survivors after a classed curated purge), refs authored in living curated files stay errors; feeds the #49 builder work. Rejected: extending the kind enumeration — the #24 drift class; tolerating everything — living curation loses its one validation. (#51)
- 2026-07-15 — §34.6/§34.3 (#51 Codex F12): "must say nothing" softened to "nothing beyond type/date/ordinal", and the correlation surface is named — a dangling date-serial id, the note's date+classes, and the grouped build report together bound "a classed record existed that day"; instance-side by design (none of the three exports), carried in the deletion page's residual inventory. Rejected: opaque random ids for classed records — the date sits in row fields and the note anyway, so only readability dies. (#51)
- 2026-07-15 — §34.3 (selfos#20 Codex F5): the purge note gains gen — a monotone content-free counter naming the operation; the composing shell's manifest/registry ack columns (purged-through, superseded-by) cite it, so crash/retry state is representable, same-day purges distinguishable, and export invalidation anchors on per-line acks instead of a date comparison (the same-day boundary problem dissolves). Rejected: a per-purge worksheet file — a second artifact whose line set can diverge from the manifest; per-line status words — two transitions per line where one integer suffices. (selfos#20)
- 2026-07-15 — §6/§10.1/§10.4/§16.4 (#33): field membership — §10.1 registry column for region kinds (concept→knowledge; zone,pattern→body; slugs canonical there); every other kind computes fields as the union of region fields reachable through its §10.4-listed refs (chains: encounter→target, plan→routes, personal_trail→segments); fields:[] renders the default field visibly flagged. Rejected: authored field: per node — hand-kept lists drift as edges change; direct-region-edges only — encounter/plan/trail focus unresolvable. (#33)
- 2026-07-15 — §10/§10.4/§20 (#32): per-kind viewer DTO — one closed §10.4 table (membership source + embedded payload) inside atlas-graph.json, which gains version:1; §16.3-driven set, bodies stay home except zone care notes and probe body; free text travels verbatim, viewer renders plain text until #37's sanitization contract. Rejected: multi-input viewer — breaks §16.4; builder-side markdown stripping — sanitization decided before the threat model; embedding full concept/material bodies — §16.3 never asks for them. (#32)
- 2026-07-15 — §9.10 (#39): influence baseline v1 — strong = artifact touches∪supports_state_updates + segment from∪to; medium = open-question pulls + encounter's target regions; weak = one authored hop (related_to|prerequisite_of|extends|contradicts|loads), never further; ordinal max, dedup sources (weak inherits its origin record), freshness §14.7 against as-of; a floor tuned only via Decision Log. Rejected: all §9.12 evidence → strong — reading paints like building; segments not counting — §9.10 lists them as an input. (#39)
- 2026-07-15 — §15.4/§15.2 (#39): frontier baseline v1 — kinds open_question/adjacent_concept/stale_concept; heads = not-stale segments (§14.7); one item per target (highest kind wins); item = {kind, target, reason, evidence≠∅} with no synthetic id — identity is (kind,target), emission sorted by target id; cap 7 config default; selection kind→date→id stays internal (§4). Rejected: future_consequence in v1 — no deterministic formula; generic stale_revisit — forks §32.5's stale_zone/stale_pattern; synthetic frontier:<slug> ids — collide across kinds and address nothing. (#39)
- 2026-07-15 — LICENSE (#26, selfos#2): MIT, decided once for the whole portfolio (selfos, atlas, exp2res, ephemeris; selfos-skills already MIT) — copyright "Агнив Банерджи (jointsome0-lgtm)"; the ecosystem-wide SECURITY.md lives in selfos, subsystem repos link to it. Rejected: Apache-2.0 — its patent grant/retaliation insures a suit profile a zero-revenue personal portfolio does not have, and the realistic takedown vectors (copied code, real personal data) are covered by provenance discipline and the hygiene gate, not by license choice; per-repo license divergence — costs more than either option. (#26)
